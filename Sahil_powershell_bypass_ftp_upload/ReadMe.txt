Make changes in the code of sahil_ftp.ps1 as per the requirement.

Currently, the ps1 file is coded to upload the targated files to remote server. 
Change the server IP and port as well as the file path which needs to exfilterate to the remote ftp server